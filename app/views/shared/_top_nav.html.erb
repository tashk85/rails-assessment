<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>                        
            </button>
            <a class="navbar-brand" href="<%= root_path %>">
                <%= image_tag "3Directions_Logo.png", alt: "3Directions Logo", class: "img-responsive" %>
            </a>
        </div>
        
        <% if user_signed_in? %>
            <div class="collapse navbar-collapse" id="myNavbar">
                <ul class="nav navbar-nav desktop">
                    <li><a href="<%= dashboard_path %>">Dashboard</a></li>
                    <li><a href="<%= listings_path %>">View Listings</a></li>
                    <li><a href="<%= my_quotes_path %>">View My Quotes</a></li>
                    <li><a href="<%= jobs_path %>">View Jobs</a></li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li><%= link_to "Log Out", destroy_user_session_path, method: "DELETE" if user_signed_in? %></li>
                </ul>
            </div>
        <% else %>
            <div class="collapse navbar-collapse" id="myNavbar">
                <ul class="nav navbar-nav">
                    <li><a href="<%= root_path({anchor: "howItWorks" }) %>">How It Works</a></li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="<%= new_user_registration_path(user_type: "printer") %>">Sign Up As A Printer</a></li>
                    <li><a href="<%= new_user_registration_path(user_type: "designer") %>">Sign Up As A Designer</a></li>
                    <li><a href="<%= new_user_session_path %>">Login</a></li>
                </ul>
            </div>
        <% end %>
    </div>
</nav>